*.blend filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.glb filter=lfs diff=lfs merge=lfs -text
# .unity scenes are text/YAML and diff efficiently in plain Git; routing them through LFS
# meant every scene edit re-uploaded a full new LFS blob, burning storage/bandwidth quota fast.
